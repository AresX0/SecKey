<UserControl x:Class="SecKey.App.Views.SecurityVaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Security Vault" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>

        <Border Grid.Row="0" Margin="180,0,0,8" Padding="8" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource StatusPanelBorderBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Master Password" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <PasswordBox x:Name="MasterPasswordBox" Width="180" PasswordChanged="MasterPasswordBox_PasswordChanged"/>
                <Button Content="Unlock" Command="{Binding UnlockVaultCommand}" Margin="8,0,0,0"/>
                <Button Content="Lock" Command="{Binding LockVaultCommand}" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,8" IsEnabled="{Binding IsUnlocked}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Name" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="2" Text="Username" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="3" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="4" Text="Secret" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="5" Text="{Binding Secret, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="6" Text="Notes" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="7" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <Grid Grid.Row="2" IsEnabled="{Binding IsUnlocked}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Save" Command="{Binding AddOrUpdateCommand}"/>
                <Button Content="Load Selected" Command="{Binding LoadSecretCommand}"/>
                <Button Content="Delete Selected" Command="{Binding RemoveSelectedCommand}"/>
                <Button Content="Clear" Command="{Binding ClearEditorCommand}"/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
            </StackPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="220"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="220"/>
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                    <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAtUtc, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="{DynamicResource SecondaryTextBrush}"/>
    </Grid>
</UserControl>
